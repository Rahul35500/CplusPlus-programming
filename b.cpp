#include <iostream>
#include <unordered_map>
using namespace std;
int main()
{
    int arr[] = {5, 6, -5, 5, 3, 5, 3, -2, 0};
    int n = sizeof(arr) / sizeof(arr[0]);
    maximum_length_sub_array(arr, n);
}
void maximum_length_sub_array(int arr[],int n)
{
    
}